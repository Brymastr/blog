#!/usr/bin/env bash

export NODE_ENV=production

rm posts.json && touch posts.json

docker-compose up -d --build

sleep 1

while ! [ -s posts.json ]; do
  echo "no posts"
  sleep 1
done

echo "got posts"

docker-compose down

next build
next export -o dist
